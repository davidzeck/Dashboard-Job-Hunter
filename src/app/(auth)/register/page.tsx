import { RegisterForm } from "@/features/auth/components";

export const metadata = {
  title: "Create Account - Job Scout",
  description: "Create your Job Scout account and start tracking opportunities",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
